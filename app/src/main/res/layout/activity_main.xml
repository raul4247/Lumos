<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background_color"
    tools:context=".MainActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/djb_im_no_wizard"
        android:text="@string/app_name"
        android:textColor="@color/text_white"
        android:layout_gravity="center_horizontal"
        android:textSize="@dimen/title_size" />

    <Button
        android:id="@+id/light_bulb_button"
        android:layout_width="@dimen/light_bulb_width"
        android:layout_height="@dimen/light_bulb_height"
        android:layout_marginTop="@dimen/light_bulb_margin_top"
        android:layout_gravity="center_horizontal"
        android:background="@drawable/light_bulb"
        android:contentDescription="@string/app_name" />
</LinearLayout>